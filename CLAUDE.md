# NCM Permission — بحث تصاريح الموظفين

تطبيق بحث عن حالة تصريح موظفي NCM. البيانات في **Supabase**، والصور في Storage.

> ⚠️ متخلطش بين المشروع ده ومشروع **PERMISSION-FROM-NCM** التاني (اللي بيقرا من Google Sheet وفيه تصاريح مركبات). الاتنين أسماؤهم قريبة بس ريبوهات وداتا مختلفة تماماً.

---

## مكوّنات المشروع

| المكوّن | المكان | الحالة |
|---|---|---|
| PWA (نسخة الويب) | جذر الريبو (`index.html`) | شغّالة إنتاج |
| تطبيق Android (غلاف WebView) | `android/` | شغّال |
| بناء الـ APK | `.github/workflows/android.yml` | شغّال |

**الريبو:** `khaledsabry255/NCM-PERMISSION-APP` — عام (Public).
**اللينك:** https://khaledsabry255.github.io/NCM-PERMISSION-APP/
**الفولدر المحلي:** `C:\Users\Security\Desktop\NCM Permission App`

> 🚨 **الريبو كان اسمه `ncm-permits-app` واتغيّر في 17 أغسطس 2026.** تغيير الاسم كسر الـ APK المثبت عند كل الناس (كان بيفتح رابط بقى 404). **متغيّرش اسم الريبو تاني** — أي تغيير محتاج APK جديد وتوزيعه من الأول.

> 🚨 **`index.html` لازم يفضل في جذر الريبو.** GitHub Pages بتدوّر عليه بالاسم ده بالظبط.

---

## مصدر البيانات

**Supabase** — جدول `employees` (725 صف: 365 موجود + 360 مستقيل) + bucket عام `employee-photos`.

الصور بتتسمى `{الكود}.jpg` و `{الكود}_2.jpg` — أي اسم مختلف = الصورة مش هتظهر.

### الأعمدة
`emp_code` · `national_id` · `full_name` · `full_name_en` · `job_title` · `department` · `category` · `phone` · `hire_date` · `address` · `permit_date` · `permit_status` · `ban_reason` · `resignation_end_date` · `notes` · `photo_path`

> 🚨 **التواريخ متخزّنة نص بصيغة `dd/mm/yyyy` مش ISO.** الكود فيه `parseDate()` بتقرا الصيغتين يدوياً وبترفض التواريخ المستحيلة زي `31/02`. متسيبش المتصفح يفسّر التاريخ لوحده — بيختلف حسب اللغة والتوقيت.

### قواعد منطقية مهمة

- **«مستقيل» = أي قيمة في `resignation_end_date`**، مش لازم تاريخ صالح. فيه صفوف مكتوب فيها `غير محدد` — دول مستقيلون فعلاً. الدالة `isResigned()` هي المرجع الوحيد لده.
- **التاريخ أقوى من الحالة المكتوبة:** صف مكتوب فيه `ساري` وتاريخه عدّى بيتعرض «تصريح منتهي». `statusOf()` هي اللي بتقرر.
- **المستقيلون مستبعدون** من تبويبات الممنوعين وقرب الانتهاء والمنتهية.
- **البحث بالأرقام تطابق تام فقط** — البحث بـ `54` مايجيبش `548`. والبحث بالاسم بيتعامل مع اختلاف رسم الحروف (أ/إ/آ/ا و ي/ى و ه/ة) عن طريق `namePattern()`.

---

## التبويبات

| التبويب | بيعرض |
|---|---|
| بحث | نتيجة البحث، ولوحة أرقام لما الخانة تكون فاضية |
| الممنوعين | `permit_status = ممنوع` وغير مستقيل |
| قرب الانتهاء | `ساري` وباقي ≤30 يوم وغير مستقيل |
| منتهية | تصريح عدّى تاريخه (سواء الحالة `منتهي الصلاحية` أو `ساري` بتاريخ قديم) وغير مستقيل |

---

## قفل الدخول

شاشة PIN من 4 أرقام (`1804` حالياً).

**مش قفل شكلي:** مفتاح Supabase نفسه **متشفّر** بالـ PIN داخل `VAULT` (PBKDF2-SHA256 بـ 300 ألف دورة → AES-256-GCM). الرقم الغلط مابيفكّش التشفير، فالتطبيق **مايقدرش يوصل لقاعدة البيانات أصلاً**.

> 🚨 **تغيير الـ PIN محتاج إعادة تشفير `VAULT` من الأول.** مش كافي تغيّر رقم في الكود — لازم يتولّد `salt` و `iv` و `data` جداد بالرقم الجديد.

**سحب الصلاحية من كل الأجهزة:** الجهاز بيخزّن `ACCESS_VERSION` في `localStorage["ncm_device_unlocked"]`. تزويد الرقم في `index.html` = كل الأجهزة تتقفل، والمفتاح المحفوظ عندها يتمسح، وتطلب الـ PIN من أول وجديد.

> 🚨 **مع أي رفع لازم تزوّد `CACHE` في `service-worker.js` كمان**، وإلا الأجهزة تفضل شغّالة على النسخة المحفوظة ومتشوفش التعديل — ولا حتى الـ `ACCESS_VERSION` الجديد.

### ثغرات معروفة ومقبولة

- الـ PIN **4 أرقام** — حد فاهم تقنياً ومعاه الملف يقدر يجرب الـ 10,000 احتمال على جهازه. بيوقف أي حد عادي، مش متخصص.
- **bucket الصور عام** — التشفير بيحمي بيانات الموظفين، لكن الصورة لو حد عرف رابطها يقدر يفتحها.
- الحل الجذري لو اتطلب: Supabase Auth + RLS مربوطة بجلسة بدل `using (true)`، وروابط صور موقّعة.

---

## اللغة

زر AR/EN في الهيدر وفي شاشة القفل. بيقلب `lang`/`dir` (RTL ↔ LTR) ويترجم الواجهة + حالات التصريح. الاختيار محفوظ في `localStorage["ncm_lang"]`.

أسماء الموظفين والوظائف والأقسام والعناوين **بتفضل زي ما هي في قاعدة البيانات** — الترجمة للواجهة والحالات بس. في الوضع الإنجليزي الاسم الإنجليزي بيبقى فوق والعربي تحته.

CSS بيستخدم خصائص منطقية (`inset-inline-*` / `text-align:end`) عشان الاتجاه يتقلب لوحده.

> 🚨 **استثناء مقصود:** زر اللغة `.langbtn` وزر التحديث `.fab` مثبتين بـ `left` و `right` **فيزيائياً**. لو اتحوّلوا لـ `inset-inline-*` هيقفزوا من ناحية للتانية مع كل تبديل لغة — وده كان بق اتصلح، والمستخدم طلب صراحةً إنهم يفضلوا ثابتين.

---

## 💰 قيد أساسي: المشروع لازم يفضل مجاني بالكامل

| الخدمة | التكلفة |
|---|---|
| GitHub Pages (استضافة الـ PWA) | مجاني للريبو العام |
| GitHub Actions (بناء الـ APK) | مجاني وبدون حد للريبو العام |
| Supabase (الداتا والصور) | مجاني للحجم الحالي |
| توزيع الـ APK من صفحة Releases | مجاني |

> 🚨 **الحاجات اللي بتتدفع ولازم نتجنبها أو ننبّه عليها:**
> - نشر على Google Play = **25 دولار** رسوم تسجيل مرة واحدة. مش مطلوب — التوزيع من Releases ببلاش.
> - لو الريبو بقى **خاص**، دقايق Actions بتبقى محدودة. **لازم يفضل عام.**
> - Supabase Pro أو أي استضافة مدفوعة — مش محتاجينها.

---

## الأمان والخصوصية

الداتا فيها **أسماء وأرقام قومية وعناوين وصور بطاقات حقيقية**. متحطش أي داتا حقيقية في ملفات المشروع، ولا رسائل commit، ولا issues، ولا أي مكان عام.

**أماكن الملفات الحساسة (كلها بره المشروع):**

| الملف | المكان |
|---|---|
| جداول الإكسل | `C:\Users\Security\Desktop\NCM DATA (خاص)` |
| مفتاح توقيع الـ APK + باسورده | `C:\Users\Security\.ncm-secrets\NCM PERMISSION - Google Play package.zip` |
| توكن GitHub | `C:\Users\Security\.ncm-secrets\github-token.txt` |

`.gitignore` بيمنع `*.xlsx` و `*.csv` و `*.keystore` و `*.zip` كخط دفاع تاني.

---

## تطبيق الأندرويد (`android/`)

package `io.github.khaledsabry255.ncmpermission`. **مجرد WebView بيفتح نفس لينك GitHub Pages ملء الشاشة** — الواجهة كلها هي `index.html` نفسه، فأي تعديل على الويب بيظهر في التطبيق تلقائي من غير APK جديد.

> 🚨 **متبنيش الواجهة تاني بـ Kotlin.** كانت متكتوبة بـ Compose قبل كده (لحد commit `e2af1a4`) والنتيجة إن كل تعديل على الويب كان لازم يتعمل مرتين، والاتنين فضلوا مختلفين. الشكل بيتظبط في `index.html` بس.

الملفات: `MainActivity.kt` (الـ WebView + هوامش الشاشة + زرار الرجوع) و `Downloads.kt` (زرار حفظ الصور — الصفحة بتحوّل الصورة لـ blob، والـ WebView مش بيعرف يحفظه، فبيترجّع بـ JavaScript ويتكتب في مجلد Downloads).

الـ APK بيتبني على GitHub Actions مع أي تعديل في `android/` وبينزل في release ثابت اسمه `latest-apk`، وموقّع بمفتاح الـ debug عشان يتثبت من اللينك على طول.

> 🚨 **مع أي تعديل في `android/` زوّد `versionCode` و `versionName`** في `android/app/build.gradle.kts`، وإلا الأندرويد ممكن يرفض يثبّت النسخة الجديدة فوق القديمة.

### الغلاف القديم (TWA)

نسخة قديمة مغلّفة بـ PWABuilder، package `io.github.khaledsabry255.twa`، لسه متثبتة عند ناس. بتفتح نفس اللينك برضه.

> 🚨 **ملف `assetlinks.json` مستضاف في ريبو منفصل** اسمه `khaledsabry255.github.io`، كملف واحد في الجذر بـ Jekyll front matter (`permalink: /.well-known/assetlinks.json`). فولدر `.well-known` المباشر **مابيشتغلش** على GitHub Pages — Jekyll بيتجاهل أي اسم بيبدأ بنقطة. الملف فيه بصمتين (المفتاح الجديد والقديم).

> 🚨 **لو مفتاح التوقيع ضاع**، أي APK جديد هيتوقّع بمفتاح مختلف، وشريط عنوان المتصفح هيرجع يظهر فوق التطبيق لحد ما تتحدّث البصمة في `assetlinks.json`.

---

## أسلوب الشغل مع خالد

- الشرح **بالعربي المصري**، وخطوات الواجهة الرسومية بالتفصيل خطوة خطوة — هو مش مبرمج.
- **ابعت ملفات كاملة، مش patches أو أجزاء.**
- قبل الكود النهائي، اعرض الشكل الأول واستنى الموافقة.
- لو فيه قرار هيغيّر شكل الشغل، اسأله قبل ما تبدأ.
