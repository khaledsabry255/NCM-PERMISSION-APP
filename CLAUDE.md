# NCM

اسم التطبيق على الموبايل **NCM** (الأيقونة: دايرة الشعار). داخل التطبيق العنوان
لسه `NCM PERMISSION`.

تطبيق واحد فيه نظامين: **الموظفين** (بيقرا من Supabase) و**التصاريح** (بيقرا من Google Sheet).
شاشة PIN واحدة، وبعدها صفحة رئيسية فيها صندوقين.

| | |
|---|---|
| **الريبو** | `khaledsabry255/NCM-PERMISSION-APP` — عام (Public) |
| **الرابط** | https://khaledsabry255.github.io/NCM-PERMISSION-APP/ |
| **الفولدر المحلي** | `C:\Users\Security\Desktop\NCM-PERMISSION` |
| **الـ APK** | https://github.com/khaledsabry255/NCM-PERMISSION-APP/releases/download/latest-apk/NCM-Permission.apk |

> ⚠️ **الفولدر اسمه `NCM-PERMISSION` والريبو اسمه `NCM-PERMISSION-APP`.** الفرق ده
> مقصود ومتساب: خالد اتسأل في 5 سبتمبر 2026 وقال إن اسم الريبو مش مهم. متغيّرش
> اسم الريبو — الرابط محفور جوّه كل APK اتوزّع، وتغييره = صفحة فاضية عند كل الناس.

> 🚨 المشروع ده بديل لمشروعين قدام كانوا منفصلين: تطبيق الموظفين (اللي كان عايش في
> نفس الريبو ده) وتطبيق التصاريح (`PERMISSION-FROM-NCM`). آخر حالة لتطبيق الموظفين
> القديم محفوظة في فرع **`before-merge`** على GitHub، وفولدرات `NCM Permission App`
> و `work sheet` لسه على الديسكتوب كنسخة احتياطية.
> **متعدّلش فيهم** — الشغل كله هنا.

---

## شكل المشروع — وأي تعديل ينزل فين

```
index.html            ← شاشة الـ PIN + الصفحة الرئيسية (الصندوقين)
employees/index.html  ← نظام الموظفين كله (HTML + CSS + JS في ملف واحد)
permits/index.html    ← نظام التصاريح كله (HTML + CSS + JS في ملف واحد)
icons/                ← أيقونات التطبيق
lib/papaparse.min.js  ← مكتبة قراءة CSV (التصاريح)
supabase.min.js       ← مكتبة Supabase (الموظفين)
manifest.json         ← بيانات الـ PWA
service-worker.js     ← الكاش
android/              ← غلاف الـ APK — ملف Kotlin واحد بس
tools/sheet-scripts/  ← سكربتات الشيت (مش جزء من الموقع)
.github/workflows/    ← بناء الـ APK
```

| عايز تعدّل | الملف |
|---|---|
| شاشة الدخول أو الصندوقين أو اللغة | `index.html` |
| أي حاجة في نظام الموظفين | `employees/index.html` |
| أي حاجة في نظام التصاريح | `permits/index.html` |
| سلوك تطبيق الأندرويد نفسه | `android/…/MainActivity.kt` |
| سكربت رفع الشيت | `tools/sheet-scripts/sync.py` |

> 🚨 **النظامين متفصلين في ملفين عن قصد — متلزقهمش في ملف واحد.**
> الاتنين بيعرّفوا `t()` و `runSearch()` و `digitsOnly()` و `ACCESS_VERSION` ومئات
> أسماء الكلاسات بنفس الأسماء وبمعاني مختلفة. لو اتحطوا في ملف واحد، التاني
> بيدهس الأول والاتنين بيتكسروا **من غير أي رسالة خطأ**.

> 🚨 **`index.html` لازم يفضل في جذر الريبو.** GitHub Pages بتدوّر عليه بالاسم ده
> بالظبط في الجذر. نقله = الموقع كله 404.

---

## قفل الدخول

شاشة PIN من 4 أرقام (`1804` حالياً)، صندوق لكل رقم، والرابع بيفتح على طول.
الرقم مبيظهرش وهو بيتكتب — نقطة على طول.

**مش قفل شكلي:** مفتاح Supabase نفسه **متشفّر** بالـ PIN داخل `VAULT` في `index.html`
(PBKDF2-SHA256 بـ 300 ألف دورة → AES-256-GCM). الرقم الغلط مابيفكّش التشفير، فنظام
الموظفين **مايقدرش يوصل لقاعدة البيانات أصلاً**.

> 🚨 **تغيير الـ PIN محتاج إعادة تشفير `VAULT` من الأول.** مش كافي تغيّر رقم في الكود.

### خالد هو الوحيد اللي يعرف الرقم

بيكتبه بنفسه مرة واحدة على كل موبايل بيسلّمه، وبعدها **مالوش لازمة تاني**. وده السبب
في حاجتين في الكود:

- **مفيش زر قفل ظاهر في الشاشة الرئيسية.** حارس يدوس عليه بالغلط = يتقفل من تطبيق
  الرقم بتاعه مش معاه. القفل بقى ضغطة مطوّلة ثانية ونص على الشعار + تأكيد.
- **المفتاح محفوظ في تلات أماكن:** `localStorage` و **IndexedDB** وكوكي (للنسخة بس).
  الموبايلات بتمسح `localStorage` لوحدها لما المساحة تقل — وده كان بق حقيقي اتصلح
  قبل كده. المفتاح **مايتحطش في الكوكي أبداً**: الكوكي بتتبعت للسيرفر مع كل طلب.

**سحب الصلاحية من كل الأجهزة:** زوّد `ACCESS_VERSION` في `index.html` **وفي
`permits/index.html` و `employees/index.html` مع بعض** — التلاتة لازم يبقوا نفس الرقم.

> 🚨 **مع أي رفع لازم تزوّد `CACHE` في `service-worker.js` كمان**، وإلا الأجهزة تفضل
> شغّالة على النسخة المحفوظة ومتشوفش التعديل — ولا حتى الـ `ACCESS_VERSION` الجديد.

---

## مصادر البيانات

### الموظفين — Supabase
جدول `employees` + bucket عام `employee-photos`. الصور بتتسمى `{الكود}.jpg` و `{الكود}_2.jpg`.

> 🚨 **التواريخ متخزّنة نص بصيغة `dd/mm/yyyy` مش ISO.** `parseDate()` بتقراها يدوياً.
> متسيبش المتصفح يفسّر التاريخ لوحده.

- **«مستقيل» = أي قيمة في `resignation_end_date`**، حتى لو مكتوب `غير محدد`. `isResigned()` هي المرجع.
- **التاريخ أقوى من الحالة المكتوبة.** `statusOf()` هي اللي بتقرر.
- **البحث بالأرقام تطابق تام فقط** — `54` مايجيبش `548`.

### التصاريح — Google Sheet
`1ZceJtmQMpW7Ky3Ysgz0mDcmE6Uxr600OuO2KOMHJnWE` بصيغة gviz وبـ **اسم التاب** مش gid.

> 🚨 **اسم تاب الأفراد فيه مسافة في الآخر:** `تصاريح أفراد ` — شيلها والتطبيق يقع.

> 🚨 **اقرا بالاسم مش بالـ gid.** أي import بيغيّر الـ gid، والقديم بيرجّع أول تاب بالغلط.

> 🚨 **متستخدمش لينكات "Publish to web".** بترجع خطأ CORS وبتوقّف التطبيق.

> 🚨 **الأعمدة بتتقرا بالـ index مش بالعنوان.** أي تغيير في ترتيبها بيكسّر التطبيق
> من غير رسالة خطأ واضحة. الأعمدة الجديدة تتضاف **في الآخر**.

---

## اللغة

زر AR/EN في **الشاشة الرئيسية بس**. الاختيار محفوظ في `localStorage["ncm_lang"]`،
والنظامين بيقروه عند الفتح ومابيغيّروهوش.

> 🚨 **هيدر التصاريح ثابت وقت البحث** (`body.search-mode .app-header{position:sticky}`)،
> والأدوات تحته بـ `--hdr-h` اللي **بتتقاس بالـ JS** في `measureHeader()`. متحطّش
> الرقم ثابت في الـ CSS — بيتغيّر مع الخطوط اللي بتتحمّل متأخر.

> ⚠️ **فرق قايم بين النظامين:** الموظفين بيقلب الاتجاه LTR في الإنجليزي، والتصاريح
> بيفضل RTL في اللغتين (قرار مقصود ومكتوب في كوده). لو اتطلب توحيدهم، ده شغل في
> CSS نظام الموظفين.

---

## داشبورد الموظفين

**خمس أرقام** (الموجودون · الاستقالات · قرب الانتهاء · منتهية · الممنوعون) كلهم
**أزرار**، وتحتيهم صندوق أزرق لكل وظيفة. كلهم بيفتحوا **كروت كاملة بترتيب الكود**.

> ⚠️ **حتى الصندوق اللي رقمه صفر بيتضغط** — بيفتح ويقول «لا يوجد». زرار ميت مش
> إجابة.

> 🚨 **صناديق الوظائف = `worksHere()`**: مش مستقيل ومش ممنوع. `loadStats` بتعدّ
> بيها و `openJob` بتعرض بيها، فالرقم والقايمة **مستحيل يختلفوا**. أي حد يستقيل أو
> يتمنع بيسقط من الاتنين لوحده. متعملش الفلتر في مكان واحد بس.

> 🚨 **الرجوع للإحصائيات من زر الموبايل، مش من زر في الصفحة.** فتح صندوق بيبدّل
> محتوى الصفحة بس، فلولا `history.pushState()` في `renderGroupList()` كان زر
> الرجوع هيخرج من النظام كله. الضغطة التانية بتخرج للصفحة الرئيسية زي ما هي.

> ⚠️ قوايم «قرب الانتهاء» و«منتهية» مرتّبة **بالكود** بطلب خالد — كانت مرتّبة
> بالأولوية (الأقرب انتهاءً الأول) وده كان أنفع للمتابعة.

> ⚠️ الشعار المصغّر بيظهر في الشريط المثبّت مع **أي تمرير** (`.toolbar.stuck`)
> مش وقت النتايج بس — الداشبورد صفحة طويلة كمان.

---

## الرجوع والنوتش

**مفيش زر رجوع يخرج من أي نظام** — الخروج من زر الموبايل نفسه. الغلاف بيعمل
`if (web.canGoBack()) web.goBack() else finish()`.

**النوتش:** النافذة بتاخد الشاشة كلها (`LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES`
مكتوب في `MainActivity.kt` **وفي `values-v27/themes.xml`** مع بعض عشان ميعتمدش على
حاجة واحدة)، وشريط الحالة وشريط التنقل ملوّنين `#EDF2F8` نفس لون الصفحة. المحتوى
بيتزحزح بعيد عن الكاميرا من الجانب الأصلي (`setOnApplyWindowInsetsListener`).

> 🚨 لأن الغلاف بيعمل الإزاحة دي بنفسه ويستهلك الـ insets، قيمة `env(safe-area-inset-*)`
> جوّه الـ WebView بتبقى **صفر**. ده صح ومقصود — من غيره كان هيبقى فيه فراغ مضاعف.
> في المتصفح العادي القيم بتشتغل طبيعي.

> 🚨 `manifest.json` على `standalone` مش `fullscreen` **عن قصد**: في fullscreen أندرويد
> بيخفي شريط التنقل، يعني الحارس مش هيلاقي زر الرجوع.

---

## 💰 المشروع لازم يفضل مجاني بالكامل

| الخدمة | التكلفة |
|---|---|
| GitHub Pages (الاستضافة) | مجاني للريبو العام |
| GitHub Actions (بناء الـ APK) | مجاني وبدون حد للريبو العام |
| Supabase + Google Sheets (الداتا) | مجاني للحجم الحالي |
| توزيع الـ APK من صفحة Releases | مجاني |

> 🚨 **لازم الريبو يفضل عام (public)** — لو بقى خاص، دقايق Actions بتتحدّد.
> ونشر Google Play = 25 دولار ومش مطلوب: التوزيع من Releases ببلاش.

---

## الأمان والخصوصية

الداتا فيها **أسماء وأرقام قومية وعناوين وصور بطاقات حقيقية**. متحطش أي داتا حقيقية
في ملفات المشروع، ولا رسائل commit، ولا issues، ولا أي مكان عام.

**أماكن الملفات الحساسة (كلها بره المشروع):**

| الملف | المكان |
|---|---|
| جداول الإكسل | `C:\Users\Security\Desktop\NCM DATA (خاص)` |
| مفتاح التوقيع + توكن GitHub | `C:\Users\Security\.ncm-secrets\` |

> 🚨 **`tools/sheet-scripts/UploadFromDesktop.gs` مالوش كلمة سر جوّاه.** الريبو عام،
> وأي قيمة في السطر ده أي حد يقدر يستخدمها ويكتب على الشيت. الحقيقية في
> `.ncm-secretspps-script-upload-secret.txt` وبتتلصق في محرّر Apps Script باليد.
> **دي كانت مكتوبة صراحةً في الريبو القديم ومكشوفة علناً** — اتشالت هنا في
> 5 سبتمبر 2026 والمفروض تتغيّر.

> 🚨 **التوكن مايتحطّش في رابط الـ remote.** ده حصل في الريبو القديم وكان بيظهر في
> أي `git remote -v`. يتقرا من `.ncm-secrets` وقت الرفع بس.

### ثغرة معروفة ومقبولة

**شيت التصاريح مشارك للعامة**، فأي حد يعرف رابطه يقدر يقرا الأسماء والأرقام القومية
من غير ما يعدّي على شاشة القفل. خالد اتشرحله ده صراحةً واختار يأجّله.
الحل المجاني لو اتطلب: Google Apps Script كـ backend، والشيت يرجع خاص.

---

## أسلوب الشغل مع خالد

- الشرح بالعربي المصري، وخطوات الواجهة الرسومية بالتفصيل خطوة خطوة.
- **ابعت ملفات كاملة، مش patches أو أجزاء.**
